name: Test Workflow No Push

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Echo runner
        run: echo "Runner draait ✅"

      - name: Create debug file
        run: echo "Debug run at $(date)" > debug-output.txt

      - name: Show repo status
        run: |
          git status
          git branch -a
          git remote -v
